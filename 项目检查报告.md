# ByUsi Studio 项目检查报告

- 检查日期：2026-09-06
- 项目版本：v1.2.0
- 项目类型：React 18 + TypeScript + Vite 6 开源项目展示站
- 检查方式：静态代码审查 + 工具链实跑（tsc / eslint / prettier / npm audit / npm outdated）+ 构建产物核验

---

## 一、总体评价

项目健康度：良好。

代码规范执行严格、架构分层清晰、构建链路完整。源码约 9,300 行（TS/TSX 约 4,500 行 + Less 约 4,900 行），包含 22 个组件、4 个自定义 Hooks、2 个 Context、完整服务层。主要问题集中在静态资源冗余和少量依赖/文档维护上，均不影响当前运行。

---

## 二、健康检查结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| TypeScript 类型检查 | 通过 | 严格模式（strict + exactOptionalPropertyTypes + noUncheckedIndexedAccess），tsc --noEmit 退出码 0，零错误 |
| ESLint | 通过 | eslint --max-warnings 0 退出码 0，0 错误 0 警告 |
| Prettier 格式 | 警告 | 3 个文件不符合格式规范：src/noJsWarning.less、.prettierrc.json、package.json；运行 npm run format 可修复 |
| 生产构建 | 通过 | dist 为 2026-09-06 08:47 构建，晚于最后源码修改时间（08:46），postbuild 部署脚本执行正常 |
| 代码卫生 | 通过 | src 目录无 TODO/FIXME/HACK/console.log 残留 |
| 依赖安全审计 | 警告 | 1 个中危漏洞：dompurify <=3.4.12（GHSA-55q2-fjhq-7xh7，XSS 风险），npm audit fix 可升至 3.4.14 |

---

## 三、需要处理的问题（按优先级）

### P0（建议尽快处理）

**1. dompurify 安全更新**

- 现状：package.json 声明 dompurify ^3.4.12，npm audit 报告中危 XSS 漏洞（IN_PLACE hook 移除后分离子树仍可执行）。
- 影响面：src/components/ReadmeModal.tsx 第 472 行使用 DOMPurify.sanitize 清洗 README HTML，第 477 行通过 dangerouslySetInnerHTML 渲染，属于该漏洞的直接影响路径。
- 修复：升级在 semver 范围内（3.4.12 -> 3.4.14），无破坏性变更。执行 `npm audit fix` 或 `npm install dompurify@^3.4.14` 后重新构建验证。

### P1（建议近期处理）

**2. 静态资源冗余约 26 MB（public 总计 28.3 MB，每次构建均会复制进 dist/deploy）**

- public/layui/（约 992 KB）：旧版 layui 框架残留（文件时间 2025-06-22）。全项目检索确认无任何对 layui.js / layui.css 的引用；组件中使用的 .layui-container 类名已在 src/App.less 第 164-168 行自行实现（max-width: 1200px 居中容器）。可整个目录删除。
- public/css/（约 5.3 MB）：iconfont 构建产物（result.css、result_original.css 及 87 个 woff2 字体文件），全项目无引用。可整个目录删除。
- FontAwesome 非运行时资源约 20 MB：运行时仅加载 Font/css/all.min.css（ui.json 配置，约 435 KB）与 Font/webfonts/（约 999 KB）；error.html 引用 Font/css/all.css。以下子目录均未被引用，可从 public 移除（如需保留建议移至仓库其他目录或依赖包管理）：
  - Font/metadata（约 10.9 MB）
  - Font/js（约 6.3 MB）
  - Font/svgs（约 1.9 MB）
  - Font/sprites（约 1.5 MB）
  - Font/less（约 238 KB）、Font/scss（约 248 KB）

**3. README 文档与实际代码不同步**

- README.md 的项目结构章节缺少 5 个实际存在的组件：GithubProjects.tsx、CommandPalette.tsx、ActivityFeed.tsx、ConfigUpdateBanner.tsx、ScrollReveal.tsx。
- 未文档化 PWA 能力：public/sw.js（Service Worker，静态资源缓存优先 + 导航网络优先回退缓存策略）与 public/manifest.webmanifest。
- 未文档化 public/css、public/icons 等静态资源说明。

### P2（知悉即可，择机处理）

**4. 后端目录缺失**

- 项目记忆中记录"后端使用 Python 实现，需启动 HTTP 服务器"，但仓库内不存在 backend/ 或 server/ 目录（.gitignore 中仍保留 server/.env 条目）。
- scripts/postbuild.mjs 第 62-68 行已将后端复制步骤降级为可选（源目录不存在时跳过且不报错），当前实际为纯静态部署。
- 建议：若后端方案已废弃，更新 .gitignore 与相关文档/记忆；若仍计划引入，补充 backend 目录说明。

**5. 依赖大版本落后（非紧急）**

- 主版本落后：React 18.3.1（最新 19.x）、Vite 6.4.3（最新 8.x）、TypeScript 5.9.3（最新 7.x）、ESLint 8.57.1（最新 10.x）、eslint-plugin-react-hooks 4.6.2（最新 7.x）、@typescript-eslint/* 7.x（最新 8.x）、@vitejs/plugin-react 4.x（最新 6.x）。
- semver 内可升级：dompurify 3.4.14、marked 18.0.11、highlight.js 11.12.0。
- 建议：先做 P0 的 dompurify 升级，大版本升级单独排期并回归验证。

**6. error.html 使用内联样式**

- public/error.html 第 8 行起包含内联 style 块。该页面为独立错误页、不经过构建管线，内联样式可保证脱离资源依赖时正常渲染，属于合理例外，但与项目"样式必须写入可编译 CSS 文件"的约束不一致，建议知悉并在文档中注明此例外。

---

## 四、架构亮点

1. API 层健壮（src/services/api.ts，331 行）：
   - 指数退避重试（最多 3 次，基准延迟 600ms），网络错误与 429/5xx 自动重试；
   - 支持 Retry-After 响应头解析（上限 10 秒）；
   - HTTP 状态码分类为用户可读中文文案；
   - localStorage 缓存 + README 缓存 LRU 淘汰（上限 30 条，防止存储膨胀）；
   - 请求失败时回退过期缓存，提升弱网可用性；
   - Gitee 与 GitHub 接口共用同一套重试/映射/去重逻辑。

2. 安全意识到位：
   - README 渲染链路为 marked -> DOMPurify 白名单清洗 -> highlight.js 代码高亮；
   - SANITIZE_CONFIG 显式放行安全排版标签，默认拦截脚本/事件属性/危险协议；
   - README 中明确警告勿在 config.json 配置 access token；
   - 一言请求带 8 秒超时（AbortController）。

3. PWA 策略合理（public/sw.js）：
   - 静态资源缓存优先 + 后台更新；
   - 导航请求网络优先、失败回退缓存首页；
   - 带查询串的请求（配置版本轮询）一律走网络，保证热更新可感知；
   - 仅生产构建注册 SW（src/main.tsx），避免干扰开发环境 HMR。

4. 工程化完整：
   - 运行时配置（ui.json / config.json）免构建调整布局与接口，配合 ConfigUpdateBanner 提示刷新；
   - Vite 手动分包（react-vendor / markdown），chunk 大小警告阈值 1500 KB；
   - TypeScript 严格度拉满，ESLint + Prettier 双闸门；
   - 首次访问 splash 加载页、ErrorBoundary 错误边界、无 JS 降级提示页（noscript）齐备。

---

## 五、建议的下一步

1. 执行 `npm audit fix`（或单独升级 dompurify 至 3.4.14），随后 `npm run build` 验证。
2. 删除 public/layui、public/css 及 FontAwesome 未使用子目录（metadata/js/svgs/sprites/less/scss），重新构建并冒烟测试图标与页面布局。
3. 执行 `npm run format` 统一 3 个文件的格式。
4. 更新 README.md：补充缺失组件说明与 PWA 章节。
5. 择机规划 React 19 / Vite 8 等大版本升级。

---

## 附录：检查证据与统计口径

- 源码规模：src 目录共 43 个文件；最大文件为 src/App.less（4,793 行 / 102.6 KB），其次为 ReadmeModal.tsx（506 行）、Projects.tsx（461 行）、api.ts（331 行）。
- public 目录体积（PowerShell Measure-Object 实测）：总计约 28.3 MB；其中 layui 991.8 KB、css 5,300.1 KB、Font/metadata 10,865.1 KB、Font/js 6,285.2 KB、Font/svgs 1,933.7 KB、Font/sprites 1,491.3 KB、Font/webfonts 999.0 KB、Font/css 434.8 KB、Font/less 238.2 KB、Font/scss 247.7 KB。
- 引用核验：全仓库检索 layui.js / layui.css / result.css 均无引用（仅 public/Font 自身文件除外）；.layui-container 类名在 src/App.less 第 164 行定义，被 9 个组件共 14 处使用。
- 构建产物：dist 与 deploy/dist 均为 2026-09-06 08:47 生成，时间晚于 src 最后修改（App.tsx 08:46），产物与源码同步。
- 工具实跑结果：tsc --noEmit 退出码 0；eslint --max-warnings 0 退出码 0；prettier --check 报告 3 个文件不符；npm audit 报告 1 个 moderate（dompurify）；npm outdated 结果见正文 P2 第 5 条。
